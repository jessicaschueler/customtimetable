[project]
name = "customtimetable"
dynamic = ["version"]
dependencies = [
    "apache-airflow",
    "flask"
]

[project.entry-points."airflow.plugins"]
my_plugin = "customtimetable:WorkdayTimetablePlugin"